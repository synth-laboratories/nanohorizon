# Candidate scratchpad log: smoke-test-apr9-d

Optimization intent:
- Tighten the private TODO list contract for Craftax to a repeatable, compact plan that tracks urgency, target, and loop-breaker.
- Preserve all shared harness surfaces and keep the change reviewable as a prompt-only candidate.

Iteration tracking (compact):
1. Establish 3-item private scratchpad with explicit stale-target refresh after 3-turn no-progress.
2. Keep action batch anchored to first todo item while preserving standard tool-call-only final output.
3. Add explicit loop-break fallback to reduce repeated movement stalls.

Open risks:
- No GEPA rollout was executed in this task due environment/tooling constraints, so score lift is unmeasured.
- If this prompt overfits to early-game only, later-stage progression may need follow-up micro-tuning.
