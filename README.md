# ActorBench NanoHorizon Craftax Hello World Worker

Actor-focused wrapper around the ReportBench `nanohorizon_craftax_hello_world`
lane.

Target actor:
- `worker`

Intent:
- run the concrete Craftax hello-world worker path and score only the worker's
  minimal rollout-and-artifact responsibilities
