2026-04-12 - Craftax Achievement Roadmap v2
- Implemented prompt shaping in `src/nanohorizon/shared/eval_model.py` with an explicit achievement roadmap and zero-reward-for-repeat framing.
- Implemented rollout context shaping in `src/nanohorizon/craftax_core/rollout.py` so each user prompt now carries `achievements_unlocked`, `next_targets`, and the current unique-achievement count.
- Restored `create_app()` in `src/nanohorizon/craftax_core/http_shim.py` with `/health`, `/task_info`, `/rollout`, and `/rollouts` endpoints; kept `run_rollout_request` as a lazy proxy to avoid an import cycle.
- Targeted Craftax tests passed: `python -m pytest tests/test_craftax_core_contract.py tests/test_craftax_core_runtime_guarantees.py tests/test_craftax_interface.py`.
- Benchmark verification blocked in this container: `jax`, `craftax`, `craftaxlm`, and `vllm` are absent, and `uv run` is pinned to a missing local `synth-ai` path.
- Baseline context for seeds 10000-10004 from `records/offline_20min_1xa100_40gb/2026-03-22_modal_4b_nochange_baseline/eval_summary.json`: mean unique achievements = `1.0`.

2026-04-12 - Craftax Achievement Roadmap v2 verification attempt
- Baseline proxy eval: `.out/baseline_proxy_eval_9016/eval_summary.json` on seeds `10000-10004`, `max_steps=4`, `max_concurrent_rollouts=1`, `gpt-4.1-nano`, local Craftax server on port `9016`.
- Candidate proxy eval: `.out/candidate_proxy_eval_9017/eval_summary.json` on the same seeds and proxy backend, local Craftax server on port `9017`.
- Both evals returned mean unique achievements `0.0` and never completed an LLM call. Observed errors were `Server disconnected without sending a response` and `All connection attempts failed`.
- The server-side 500s came from the local Craftax dependency state, not from the candidate prompt: the texture cache bundled with `craftax` had to be cleared, and the eval commands had to use `uv run --no-sync` to avoid rehydrating the broken cache.
- The candidate prompt change is still the intended direction, but this run is blocked from a meaningful score comparison in this container.
