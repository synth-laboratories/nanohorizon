2026-04-07 - Craftax todo-tool candidate packaging
- Scope kept to prompt-opt assets; protected shared harness files were left unchanged.
- Refined `configs/craftax_prompt_opt_qwen35_4b_codex_durable_intent_fix.yaml` so the private three-item todo list now explicitly refreshes completed items every turn and swaps targets after repeated no-progress loops while preserving the strict `craftax_interact` tool contract.
- Updated the matching record bundle under `records/prompt_opt_1usd_gpt54_family/2026-04-07_codex_durable_intent_fix/` so `run_config.yaml` and `notes.md` reflect the same candidate wording.
- Validation run: `uv run pytest tests/test_codex_durable_intent_candidate.py` passed on 2026-04-07.
- Validation run: `uv run python -m nanohorizon.shared.validate_record records/prompt_opt_1usd_gpt54_family/2026-04-07_codex_durable_intent_fix` returned `ok: true` on 2026-04-07.
- Local reviewable commit: `0434db4681ae114669540ab37dd4211c00f20645` (`Preserve Craftax todo guidance during prompt optimization`).
- Push blocker: `workspace_push` failed with `workspace_push requires a live worker session; no live worker handle was found`, so this run can leave a local reviewable commit but cannot complete the required backend-tracked push flow.
- Caveat: no live Craftax or Modal rollout run was executed in this task, so score impact remains unmeasured.

2026-04-07 - Craftax todo refresh gate candidate
- Kept the change inside `src/nanohorizon/baselines/prompt_opt.py`, one new prompt-opt config, one new record bundle, and a focused regression test; protected shared Craftax harness files remained unchanged.
- Centralized the todo-tool contract in `src/nanohorizon/baselines/prompt_opt.py` via `TODO_SCRATCHPAD_REQUIREMENTS` so GEPA reflection and rollout feedback preserve the same private three-item scratchpad semantics instead of drifting across duplicated wording.
- Added `configs/craftax_prompt_opt_qwen35_4b_codex_todo_refresh_gate.yaml`, which asks the policy to refresh its private three-item todo list every turn, replace stale targets after no-progress loops, and make the 3-4 action batch follow the current first todo item.
- Packaged the matching record bundle under `records/prompt_opt_1usd_gpt54_family/2026-04-07_codex_todo_refresh_gate/` with `command.txt`, `metadata.json`, `metrics.json`, `notes.md`, `run_config.yaml`, and `system_info.json`.
- Validation run: `uv run pytest tests/test_codex_todo_refresh_gate_candidate.py` passed on 2026-04-07.
- Validation run: `uv run python -m nanohorizon.shared.validate_record records/prompt_opt_1usd_gpt54_family/2026-04-07_codex_todo_refresh_gate` returned `ok: true` on 2026-04-07.
- Backend-tracked push/result is finalized separately from these repo notes; inspect the run state or final handoff for the pushed commit outcome.
- Caveat: this candidate was packaged and smoke-checked only; no live Craftax reward run was executed, so impact is still unmeasured.

2026-04-08 - Craftax todo string fix E2E candidate
- Kept the change inside `src/nanohorizon/baselines/prompt_opt.py`, one new prompt-opt config, one new record bundle, one focused regression test, and handoff docs; protected shared Craftax harness files remained unchanged.
- Strengthened `TODO_SCRATCHPAD_REQUIREMENTS` so prompt reflection and rollout feedback now preserve two extra constraints: each todo item stays a short string fragment, and the 3-4 action batch advances the current first todo item end-to-end instead of mixing unrelated goals.
- Added `configs/craftax_prompt_opt_qwen35_4b_codex_todo_string_fix_e2e.yaml` and the matching record bundle under `records/prompt_opt_1usd_gpt54_family/2026-04-08_codex_todo_string_fix_e2e/`.
- Comparison evidence recorded in the candidate bundle: `records/prompt_opt_1usd_gpt54_family/2026-03-21_reference_baseline/metrics.json` shows `bootstrap_score=0.6` and `primary_score=0.35`, which supports choosing another narrow prompt-only correction over a harness edit.
- `uv run pytest ...` with the default project environment is currently blocked in this workspace because `uv.lock` resolves the optional `synth-ai` dependency to a missing local path (`../synth-ai` / `/Users/joshpurtell/Documents/GitHub/synth-ai`); use `PYTHONPATH=/workspace/src uv run --no-project --with pytest --with pyyaml ...` as the current reproducible workaround for focused local validation.
- Focused validation on 2026-04-08 passed with the workaround: `PYTHONPATH=/workspace/src uv run --no-project --with pytest --with pyyaml python -m pytest tests/test_codex_todo_string_fix_e2e_candidate.py` and `PYTHONPATH=/workspace/src uv run --no-project --with pyyaml python -m nanohorizon.shared.validate_record records/prompt_opt_1usd_gpt54_family/2026-04-08_codex_todo_string_fix_e2e`.
- Packaging fix from this run: the 2026-04-08 record bundle originally pointed to a missing config and missing regression test. Added `configs/craftax_prompt_opt_qwen35_4b_codex_todo_string_fix_e2e.yaml` and `tests/test_codex_todo_string_fix_e2e_candidate.py` so the bundle is now internally consistent and reviewable.
- Updated validation after the packaging fix:
  `PYTHONPATH=/workspace/src uv tool run --with pytest --with pyyaml pytest tests/test_codex_todo_string_fix_e2e_candidate.py tests/test_codex_todo_refresh_gate_candidate.py tests/test_codex_durable_intent_candidate.py` => `9 passed`
  `PYTHONPATH=/workspace/src uv run --no-sync python -m nanohorizon.shared.validate_record records/prompt_opt_1usd_gpt54_family/2026-04-08_codex_todo_string_fix_e2e` => `ok: true`
- Review artifact: `records/prompt_opt_1usd_gpt54_family/2026-04-08_codex_todo_string_fix_e2e/verifier_review.md` now records the real blocker: the repo package is consistent, but the candidate is still `not_ready` because no live runtime/reward evidence was available in this workspace.

2026-04-08 - Craftax todo string Fix E2E
- Kept the new change set to `src/nanohorizon/baselines/prompt_opt.py` plus the focused regression test in `tests/test_codex_todo_refresh_gate_candidate.py`; protected Craftax harness files remained untouched.
- Added `enforce_todo_scratchpad_contract(...)` so the canonical three-item todo/scratchpad contract is re-applied to the seed prompt, every evaluated candidate prompt, and the persisted best candidate prompt. This closes an end-to-end string-preservation gap where GEPA reflection could drift away from the todo wording despite the reflection instructions asking it not to.
- Verification that succeeded in this workspace:
  `PYTHONPATH=/workspace/src uv tool run --with pytest --with pyyaml pytest tests/test_codex_todo_refresh_gate_candidate.py`
  `PYTHONPATH=/workspace/src uv tool run --with pyyaml python -m nanohorizon.shared.validate_record records/prompt_opt_1usd_gpt54_family/2026-04-07_codex_todo_refresh_gate`
  `git diff --check -- src/nanohorizon/baselines/prompt_opt.py tests/test_codex_todo_refresh_gate_candidate.py`
- Verifier-style feedback:
  `uv tool run --with ruff ruff check src/nanohorizon/baselines/prompt_opt.py tests/test_codex_todo_refresh_gate_candidate.py` failed on pre-existing import-layout and duplicate-import findings in `src/nanohorizon/baselines/prompt_opt.py`; this fix did not broaden scope to clean that older file-wide lint debt.
- Environment caveat:
  plain `uv run ...` still fails in this workspace because `pyproject.toml` pins `synth-ai @ file:///Users/joshpurtell/Documents/GitHub/synth-ai`, which is not mounted here. Used `uv tool run` instead for reproducible targeted verification.
- Remaining product caveat: no live Craftax reward run was executed, so this change proves stronger todo-contract preservation end-to-end but does not yet measure leaderboard impact.
