2026-04-10 - Craftax Test Candidate publication
- Published the candidate branch to GitHub as `test-candidate-final` and opened PR `#19` (`https://github.com/synth-laboratories/nanohorizon/pull/19`).
- The published branch tip is the current reviewable head for the candidate chain on `test-candidate-final`.
- Fresh validation in this workspace passed:
  - `PYTHONPATH=src uv run --no-project --with pytest --with pyyaml python -m pytest tests/test_codex_durable_intent_candidate.py tests/test_codex_todo_refresh_gate_candidate.py -q`
  - `PYTHONPATH=src uv run --no-project --with pytest --with pyyaml --with httpx --with fastapi --with uvicorn --with pillow --with numpy --with craftax python -m pytest tests/test_craftax_core_contract.py tests/test_craftax_core_runtime_guarantees.py -q`
- No live Craftax leaderboard score run was executed, so the performance gain remains unmeasured.

2026-04-10 - Craftax Test Candidate working-memory buffer
- Scope kept to the shared Craftax harness surfaces listed in the task contract; the rollout HTTP surface, tool schema, and action catalog stayed stable.
- Implemented a compact working-memory buffer in `src/nanohorizon/craftax_core/metadata.py` and threaded it through `src/nanohorizon/craftax_core/rollout.py` so each follow-up prompt sees the previous turns' plan, actions, compact state summary, reward, and achievements.
- Added a regression test in `tests/test_craftax_core_contract.py` that verifies the second prompt contains the rendered working-memory block from turn 0.
- Updated `docs/task-craftax.md` with a brief candidate note so later reviewers can see the intended strategy without searching the run logs.
- Validation passed with `uv run --no-project --with pytest --with fastapi --with httpx --with pillow --with pyyaml --with numpy python -m pytest tests/test_craftax_core_contract.py -q` on 2026-04-10.
- Remaining packaging steps: create the reviewable commit on `test-candidate-final`, push the branch, and open the GitHub PR.

2026-04-07 - Craftax todo-tool candidate packaging
- Scope kept to prompt-opt assets; protected shared harness files were left unchanged.
- Refined `configs/craftax_prompt_opt_qwen35_4b_codex_durable_intent_fix.yaml` so the private three-item todo list now explicitly refreshes completed items every turn and swaps targets after repeated no-progress loops while preserving the strict `craftax_interact` tool contract.
- Updated the matching record bundle under `records/prompt_opt_1usd_gpt54_family/2026-04-07_codex_durable_intent_fix/` so `run_config.yaml` and `notes.md` reflect the same candidate wording.
- Validation run: `uv run pytest tests/test_codex_durable_intent_candidate.py` passed on 2026-04-07.
- Validation run: `uv run python -m nanohorizon.shared.validate_record records/prompt_opt_1usd_gpt54_family/2026-04-07_codex_durable_intent_fix` returned `ok: true` on 2026-04-07.
- Local reviewable commit: `0434db4681ae114669540ab37dd4211c00f20645` (`Preserve Craftax todo guidance during prompt optimization`).
- Push blocker: `workspace_push` failed with `workspace_push requires a live worker session; no live worker handle was found`, so this run can leave a local reviewable commit but cannot complete the required backend-tracked push flow.
- Caveat: no live Craftax or Modal rollout run was executed in this task, so score impact remains unmeasured.

2026-04-10 - Craftax working-memory buffer candidate
- Added a bounded working-memory buffer to the shared Craftax rollout path via `src/nanohorizon/craftax_core/metadata.py` and `src/nanohorizon/craftax_core/rollout.py`; follow-up prompts now receive recent plan/action/state summaries and `working_memory_capacity` is forwarded explicitly in rollout requests.
- Updated `src/nanohorizon/baselines/prompt_opt.py` and `configs/craftax_prompt_opt_qwen35_4b_codex_todo_refresh_gate.yaml` so the prompt-opt candidate describes the private three-item buffer as a working-memory contract while preserving the existing tool schema and rollout shape.
- Updated `records/prompt_opt_1usd_gpt54_family/2026-04-07_codex_todo_refresh_gate/` notes and run config to match the buffer-aware candidate.
- Validation: `PYTHONPATH=src uv run --no-project --with pytest --with pyyaml python -m pytest tests/test_codex_durable_intent_candidate.py tests/test_codex_todo_refresh_gate_candidate.py` passed.
- Validation: `PYTHONPATH=src uv run --no-project --with pytest --with pyyaml --with httpx --with fastapi --with uvicorn --with pillow --with numpy --with craftax python -m pytest tests/test_craftax_core_contract.py tests/test_craftax_core_runtime_guarantees.py` passed after fixing the working-memory fixture.
- Validation: `PYTHONPATH=src uv run --no-project python -m nanohorizon.shared.validate_record records/prompt_opt_1usd_gpt54_family/2026-04-07_codex_durable_intent_fix` and `.../2026-04-07_codex_todo_refresh_gate` both returned `ok: true`.
- Caveat: project-discovering `uv run` still hits a stale optional `synth-ai` file dependency; the verified commands used `--no-project` to avoid that unrelated metadata issue.
- Caveat: no live Craftax reward run was executed, so leaderboard impact remains unmeasured.
- Commit chain: implementation commit `f9484e8` plus handoff commit `8e4ffb8d6d8485a7eca353ef028a11839f3f4303` are now on `worker/run-59422a8a-13fa-4cfa-922c-552509e22be0`.

2026-04-07 - Craftax todo refresh gate candidate
- Kept the change inside `src/nanohorizon/baselines/prompt_opt.py`, one new prompt-opt config, one new record bundle, and a focused regression test; protected shared Craftax harness files remained unchanged.
- Centralized the todo-tool contract in `src/nanohorizon/baselines/prompt_opt.py` via `TODO_SCRATCHPAD_REQUIREMENTS` so GEPA reflection and rollout feedback preserve the same private three-item scratchpad semantics instead of drifting across duplicated wording.
- Added `configs/craftax_prompt_opt_qwen35_4b_codex_todo_refresh_gate.yaml`, which asks the policy to refresh its private three-item todo list every turn, replace stale targets after no-progress loops, and make the 3-4 action batch follow the current first todo item.
- Packaged the matching record bundle under `records/prompt_opt_1usd_gpt54_family/2026-04-07_codex_todo_refresh_gate/` with `command.txt`, `metadata.json`, `metrics.json`, `notes.md`, `run_config.yaml`, and `system_info.json`.
- Validation run: `uv run pytest tests/test_codex_todo_refresh_gate_candidate.py` passed on 2026-04-07.
- Validation run: `uv run python -m nanohorizon.shared.validate_record records/prompt_opt_1usd_gpt54_family/2026-04-07_codex_todo_refresh_gate` returned `ok: true` on 2026-04-07.
- Backend-tracked push/result is finalized separately from these repo notes; inspect the run state or final handoff for the pushed commit outcome.
- Caveat: this candidate was packaged and smoke-checked only; no live Craftax reward run was executed, so impact is still unmeasured.
