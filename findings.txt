2026-04-07 - Craftax todo-tool candidate packaging
- Scope kept to prompt-opt assets; protected shared harness files were left unchanged.
- Refined `configs/craftax_prompt_opt_qwen35_4b_codex_durable_intent_fix.yaml` so the private three-item todo list now explicitly refreshes completed items every turn and swaps targets after repeated no-progress loops while preserving the strict `craftax_interact` tool contract.
- Updated the matching record bundle under `records/prompt_opt_1usd_gpt54_family/2026-04-07_codex_durable_intent_fix/` so `run_config.yaml` and `notes.md` reflect the same candidate wording.
- Validation run: `uv run pytest tests/test_codex_durable_intent_candidate.py` passed on 2026-04-07.
- Validation run: `uv run python -m nanohorizon.shared.validate_record records/prompt_opt_1usd_gpt54_family/2026-04-07_codex_durable_intent_fix` returned `ok: true` on 2026-04-07.
- Local reviewable commit: `0434db4681ae114669540ab37dd4211c00f20645` (`Preserve Craftax todo guidance during prompt optimization`).
- Push blocker: `workspace_push` failed with `workspace_push requires a live worker session; no live worker handle was found`, so this run can leave a local reviewable commit but cannot complete the required backend-tracked push flow.
- Caveat: no live Craftax or Modal rollout run was executed in this task, so score impact remains unmeasured.

2026-04-07 - Craftax todo refresh gate candidate
- Kept the change inside `src/nanohorizon/baselines/prompt_opt.py`, one new prompt-opt config, one new record bundle, and a focused regression test; protected shared Craftax harness files remained unchanged.
- Centralized the todo-tool contract in `src/nanohorizon/baselines/prompt_opt.py` via `TODO_SCRATCHPAD_REQUIREMENTS` so GEPA reflection and rollout feedback preserve the same private three-item scratchpad semantics instead of drifting across duplicated wording.
- Added `configs/craftax_prompt_opt_qwen35_4b_codex_todo_refresh_gate.yaml`, which asks the policy to refresh its private three-item todo list every turn, replace stale targets after no-progress loops, and make the 3-4 action batch follow the current first todo item.
- Packaged the matching record bundle under `records/prompt_opt_1usd_gpt54_family/2026-04-07_codex_todo_refresh_gate/` with `command.txt`, `metadata.json`, `metrics.json`, `notes.md`, `run_config.yaml`, and `system_info.json`.
- Validation run: `uv run pytest tests/test_codex_todo_refresh_gate_candidate.py` passed on 2026-04-07.
- Validation run: `uv run python -m nanohorizon.shared.validate_record records/prompt_opt_1usd_gpt54_family/2026-04-07_codex_todo_refresh_gate` returned `ok: true` on 2026-04-07.
- Backend-tracked push/result is finalized separately from these repo notes; inspect the run state or final handoff for the pushed commit outcome.
- Caveat: this candidate was packaged and smoke-checked only; no live Craftax reward run was executed, so impact is still unmeasured.

2026-04-12 - Craftax recent-trajectory prompt candidate
- Added a compact recent-trajectory block to `src/nanohorizon/craftax_core/rollout.py` so each new user prompt can see the last few action batches with reward / return-to-go context and the loop-break guidance can react to actual trajectory history.
- Restored the missing FastAPI shim in `src/nanohorizon/craftax_core/http_shim.py` with `/health`, `/task_info`, `/rollout`, and `/rollouts` while keeping the rollout handler as a lazy wrapper to avoid the `http_shim`/`runner` import cycle.
- Focused verifier run: `PYTHONPATH=src python -m pytest tests/test_craftax_core_contract.py tests/test_craftax_interface.py` passed after the shim fix.
- Proxy baseline-vs-candidate comparison: `records/prompt_opt_1usd_gpt54_family/2026-04-12_recent_trajectory_proxy/` records a three-seed direct-rollout comparison on the fake Craftax runner because the real `craftax` package is not installed in this checkout.
- Proxy result: baseline mean outcome reward `1.0`, candidate mean outcome reward `2.0`, delta `+1.0` on seeds `10000-10002`; the candidate reached `collect_sapling` in addition to `collect_wood` in all three proxy rollouts.
- Caveat: no live Craftax runtime eval was possible in this workspace because the `craftax` dependency was unavailable, so the comparison is a prompt-shape proxy rather than a real-model leaderboard score.

2026-04-12 - Craftax compact follow-first candidate
- Added `configs/craftax_prompt_opt_qwen35_4b_codex_compact_follow_first.yaml` and the matching placeholder record bundle under `records/prompt_opt_1usd_gpt54_family/2026-04-12_codex_compact_follow_first/`.
- Tightened `src/nanohorizon/baselines/prompt_opt.py` so the todo contract and reflection feedback explicitly stay compact and action-directed instead of expanding into a general planning rubric.
- Added `tests/test_codex_compact_follow_first_candidate.py`; updated `tests/test_codex_durable_intent_candidate.py` to match the new reflection wording.
- Structural validation passed with `PYTHONPATH=src uv run --no-project --with pytest --with pyyaml --with httpx --with fastapi --with numpy --with pillow --with modal --with gepa pytest ...` and `uv run ... validate_record ...`.
- Proxy benchmark artifact: `experiments/craftax_prompt_opt_compact_follow_first/results/proxy_compare.json`.
- Proxy benchmark command: `PYTHONPATH=src uv run --no-project --with pytest --with pyyaml --with httpx --with fastapi --with numpy --with pillow --with modal --with gepa python experiments/craftax_prompt_opt_compact_follow_first/benchmark_proxy.py`.
- Proxy benchmark result on 8 repeated eval-seed rollouts per prompt: baseline mean_outcome_reward 0.0625, candidate mean_outcome_reward 1.0, delta +0.9375.
- Operational note: `uv run` in project mode tried to resolve the optional `cloud` path dependency at `/Users/joshpurtell/Documents/GitHub/synth-ai`; `--no-project` with explicit packages was the workable path for local validation in this workspace.
